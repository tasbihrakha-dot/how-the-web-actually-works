 hello tasbih 
 fs.readFile("test1.txt", "utf8", (err, data) => {
    if (err) {
        console.log(err);
        return;
    }

    text1 = data;
    count++;

    if (count === 2) {
        event.emit("filesReady");
    }
});